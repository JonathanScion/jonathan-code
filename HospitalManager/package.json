{
  "name": "hospital-manager",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install:all": "cd backend && npm install && cd ../frontend && npm install",
    "dev": "concurrently \"cd backend && npm run dev\" \"cd frontend && npm run dev\"",
    "dev:backend": "cd backend && npm run dev",
    "dev:frontend": "cd frontend && npm run dev",
    "build": "cd backend && npm run build && cd ../frontend && npm run build",
    "db:migrate": "cd backend && npx prisma migrate dev",
    "db:seed": "cd backend && npx prisma db seed",
    "db:reset": "cd backend && npx prisma migrate reset --force",
    "test:unit": "cd backend && npm run test:unit && cd ../frontend && npm run test",
    "test:integration": "cd backend && npm run test:integration",
    "test:contract": "cd backend && npm run test:contract",
    "test:e2e": "cd e2e && npx playwright test",
    "test:smoke": "cd backend && npm run test:smoke",
    "test:all": "npm run test:unit && npm run test:contract && npm run test:integration && npm run test:e2e",
    "test:db:up": "docker compose -f docker-compose.test.yml up -d --wait",
    "test:db:down": "docker compose -f docker-compose.test.yml down -v",
    "test:db:migrate": "cd backend && cross-env DATABASE_URL=postgresql://postgres:postgres@localhost:5434/hospital_test npx prisma db push --force-reset --skip-generate"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "cross-env": "^10.1.0"
  }
}
